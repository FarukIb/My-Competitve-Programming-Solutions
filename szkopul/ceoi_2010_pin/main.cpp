#include <bits/stdc++.h>
#define pii pair<int, int>
#define ll long long
#define ld long double

using namespace std;

map<string, int> mapp;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n, d;
    cin >> n;
    cin >> d;

    for (int i = 0; i < n; i++) {
        string x;
        cin >> x;
        mapp[x]++;
    }

    for (int mask = 0; mask < )
}
