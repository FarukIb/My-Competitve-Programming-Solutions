#include <bits/stdc++.h>

using namespace std;

typedef long double ld;
const ld PI = 3.14159265359;

int main() {
    ld xc, yc, l;
    cin >> xc >> yc >> l;
    
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) {
        int x, y;
        cin >> x >> y;
    }
    
    cout << fixed << setprecision(5) << l * l * PI << endl;
    
    return 0;
}