#include <bits/stdc++.h>
#define mp make_pair

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

const double eps = 1e-6;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int n;
    cin >>n;

    double x, y;

    if (abs(x - y) <= eps) {
        
    }
}